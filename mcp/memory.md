# mcp_servers.memory 本地记忆服务

## 说明

本地知识图谱记忆 MCP 服务。

依赖: Node / npm / npx

## 安装命令

```toml
[mcp_servers.memory]
command = "npx"
args = ["-y", "@modelcontextprotocol/server-memory"]
startup_timeout_sec = 120.0
```
