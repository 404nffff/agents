| 接口地址 | 入参 | 出参 | 测试条件 | 边界条件 | 测试结果 | 请求时间 | 关联脚本文件 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `{{method}} {{url}}`<br>`Step {{step}}：{{step_title}}`<br>{{接口说明}} | JSON：`{{request_json}}`<br>字段说明：<br>{{request_field_descriptions}} | JSON（原始 `{{business_method}}` 返回 `data`）：`{{business_response_json}}`<br>字段说明：<br>{{business_response_field_descriptions}} | {{测试数据来源}}<br>{{配置条件}}<br>{{样本条件}} | {{边界条件}}<br>{{执行边界}}<br>{{步骤边界}} | {{步骤状态}}<br>{{断言状态}}<br>{{断言说明}}<br>{{耗时}} | {{executed_at}} | `{{probe_script_path}}` |

<!--
使用规则：
1. 结果文件只保留上方 Markdown 表格，不输出标题、备注、说明段落等非表格内容。
2. 每次运行链路探针时，在同一个结果文件尾部追加一行或多行；如旧文件不是本模板表头，先重置为本模板表头再写入。
3. 入参、出参单元格必须同时包含 JSON 和字段说明；字段说明按 `字段名：含义/来源/约束` 编写。
4. 出参 JSON 必须取当前接口或业务方法的原始返回值，也就是链路探针结果 JSON 中对应 step 的 `data` 字段；禁止把 `status`、`assertion`、`verification`、`elapsed_ms`、`step` 等链路探针外层包装字段写入出参。
5. 如果原始业务方法返回数组、对象、分页结构或错误结构，出参必须保持原始层级、字段名和字段值，不得为了展示而重命名、摘要化、扁平化、补默认值或合并字段；断言、自检和是否有 bug 只能写入测试结果列。
6. 测试条件只写数据来源、配置条件、样本条件和环境条件；禁止把边界条件混入测试条件列。
7. 边界条件单独写入边界条件列，例如空值、默认值、越界值、权限边界、执行开关、dry-run、分页上限、只读/写入边界。
8. 复杂 JSON 必须压缩为单行 JSON；单元格内换行使用 `<br>`，竖线使用 `&#124;` 或等价转义，避免破坏表格。
9. 关联脚本文件写项目相对路径，例如 `docs/[需求目录]/[需求标识]_probe.php`。
-->
